<head>
    <link rel="stylesheet" href="~/css/selection/index.css">
</head>


<form method="post" action="@Url.Action("Winner", "Tournament")">
<div class="header-container">
  <img src="~/img/fundinho.png" alt="Imagem de Cabeçalho" class="header-image">
  <div class="button-container">
    <div class="button">
      <button class="btn btn-success" type="submit">Começar</button>
    </div>
    <div class="button">
      <input type="button" value="Selecione todos" class="btn btn-primary" id="select-all"/>
    </div>
    <div>
      <a class="btn btn-danger" asp-controller="pokedex" asp-action="index">Pokedex</a>
    </div>
    <div>
      <a class="btn btn-dark" asp-controller="home" asp-action="index">Página inicial</a>
    </div>
  </div>
</div>

<div class="row">
    <label hidden>Selecione os 16 lutadores</label>
@foreach (var pokemon in Model) {
    <div class="col-md-3">
        <div class="custom-control custom-checkbox image-checkbox">
            <input type="checkbox" class="custom-control-input" id="@pokemon.Id" name="@pokemon.Id">
            <span class="pokemon-name">@pokemon.PokeName</span>
            <label class="custom-control-label" for="@pokemon.Id">
                <img src="~/img/@(pokemon.Id).jpg" alt="#" class="img-fluid">                                
            </label>
        </div>
    </div>
}
</div>

</form>

<script src="js/selection/index.js"></script>